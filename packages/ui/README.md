# @crmflow/ui

Shared UI component library for CRMFlow.

## Usage

Import components from this package in your Next.js app:

```tsx
import { Button } from '@/ui'
```

## Development

Add new components to the `components/` directory and export them from `index.ts`.
